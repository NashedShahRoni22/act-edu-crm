import Settingspage from "@/components/dashboard/settings/Settingspage";

export const metadata = {
  title: "Settings | ACT Education CRM",
  description: "Configure system settings, preferences, and organization details.",
};

export default function page() {
  return <Settingspage />;
}